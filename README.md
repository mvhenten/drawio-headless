# PR #11 screenshots

Static asset branch for [PR #11](https://github.com/mvhenten/drawio-headless/pull/11).

This is an orphan branch that holds only the inline PNG screenshots embedded in
that PR description. It has no shared history with `main` and is not intended
for merge. Deleting this branch will break the image links in the PR body.
